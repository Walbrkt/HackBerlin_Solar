# Runtime — inference server
# Pins below are CVE-aware minimums; bump only after testing.
fastapi>=0.115            # closes a few starlette-related advisories.
uvicorn[standard]>=0.30
pydantic>=2.7
starlette>=0.40           # CVE-2024-47874 (DoS via multipart) → fixed in 0.40+
python-multipart>=0.0.18  # CVE-2024-53981 (DoS in form parsing)
joblib>=1.4               # 1.4 ships hardened pickle handling.
numpy>=2.0                # numpy <2 had a heap-overflow CVE; 2.x is patched.
pandas>=2.2
scikit-learn>=1.5         # CVE-2024-5206 (sample weight info leak) → fixed in 1.5
google-genai>=1.0         # /api/design-system/from-prompt — Gemini-powered feature extraction
python-dotenv>=1.0        # loads .env into os.environ at server startup

# Training — model + plots (only needed when running src/train_model.py)
matplotlib>=3.9           # CVE in 3.7 path handling → 3.9+ patched
seaborn>=0.13

# Test client — src/test_inference_server.py
requests>=2.32.4          # CVE-2024-47081 (control-flow when proxy in URL) → 2.32.4+
